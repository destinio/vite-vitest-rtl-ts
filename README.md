# Vite / React / Vitest / RTL / TypeScript

## Get up and running

```sh
pnpm i
```

```json
"scripts": {
  "dev": "vite",
  "build": "tsc && vite build",
  "preview": "vite preview",
  "test": "vitest",
  "test:c": "vitest run --coverage"
},
```
